# 贡献指南

感谢你关注好律狮法律技能包。欢迎提交错误修复、兼容性改进、测试用例、文档改进和新的编排能力。

## 提交前检查

请确保：

1. 改动只涉及技能源码、编排文档、测试或仓库协作文件，不包含官网、宣传素材、内部发布配置或构建产物。
2. 不提交真实姓名、身份证号、联系方式、合同全文、案件材料、访问令牌、服务端密钥或本地路径。
3. 已运行 `npm run check` 和 `npm test`。
4. 如果改动了交互协议、接口字段或工作流规则，同时更新对应的 `references/` 文档和测试。
5. 对用户可见的行为变化，在合并请求中说明兼容性影响和升级方式。

## 开发方式

```bash
git clone git@github.com:mtcto/haolvshi-legal-skill.git
cd haolvshi-legal-skill
cd skills/haolvshi-legal-skill
npm install
npm run check
npm test
```

本项目当前没有 npm 依赖，`npm install` 主要用于保持标准 Node.js 项目工作流；不需要为了运行测试安装额外服务。

## 提交合并请求

- 一个合并请求尽量只解决一个主题。
- 标题使用简短、明确的中文，例如“修复合同审核超时恢复”或“补充交通事故材料测试”。
- 描述问题背景、解决方案、测试结果和潜在影响。
- 涉及法律流程的改动，请说明信息来源、适用范围和是否需要人工复核。
- 不要为了让测试通过而放宽证据边界、隐私规则或错误处理约束。

维护者会重点检查用户数据边界、跨任务隔离、接口失败处理、交互字段完整性和文书生成前的确认流程。
