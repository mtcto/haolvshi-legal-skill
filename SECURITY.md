# 安全政策

## 支持范围

当前主分支和最新公开版本优先获得安全问题处理。

## 报告安全问题

请不要在公开 Issue 中发布可利用的漏洞、真实用户数据、合同全文、身份证件、访问令牌或服务端配置。

请发送邮件至 `MD@LVPIN100.COM`，标题建议包含“好律狮法律技能包安全问题”，并提供：

- 受影响的版本或提交；
- 复现步骤和最小化示例；
- 可能的影响范围；
- 可行的修复建议（如有）。

请对所有案例和日志进行脱敏。维护者会在确认收到后评估问题，并在适当范围内同步修复进展。请不要通过漏洞报告要求处理具体法律案件。

## 使用者安全建议

- 不要把密钥、令牌或真实个人信息写入命令行历史、Issue、合并请求或测试夹具。
- 仅向可信的服务地址发送必要的案件材料。
- 使用自己的服务时，通过环境变量配置服务地址，不要把服务端密钥写入技能源码。
- 对 AI 生成的法律报告和文书进行人工核验，不要将其视为自动生效的法律意见。
